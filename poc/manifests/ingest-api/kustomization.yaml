apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dashi-web

resources:
  - namespace.yaml
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: dashi
    includeSelectors: false
